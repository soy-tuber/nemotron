# 判断エンドポイントの依存 / Dependencies for the decision endpoint.
# vLLM本体とは独立。ゲートウェイにはHTTPで話すだけ。
# Independent of vLLM itself -- this only speaks HTTP to the gateway.
httpx>=0.27
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.29
